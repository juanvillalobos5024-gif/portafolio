'use client';
import { usePathname } from 'next/navigation';
import styles from './WhatsAppButton.module.css';

export default function WhatsAppButton({ collection = "", inline = false }: { collection?: string, inline?: boolean }) {
  return null;
}
